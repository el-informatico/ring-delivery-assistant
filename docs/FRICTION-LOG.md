# Friction log — Ring Delivery & Safety Assistant

Optional Devpost entry worth up to a **+10 % judging bonus** ("Participants
who submit friction logs score higher in judging (up to 10% bonus)",
amazonappdev2026.devpost.com/rules). Format = the official field list,
verbatim: "specific task attempted, steps taken, expected vs. actual
result, severity rating, workaround used, and actionable suggestion."
On submission day, paste one row per form entry.

Honesty rules (same binding as SUBMISSION.md): every row cites where the
finding is documented in this repo; anticipated-but-not-yet-experienced
friction is marked **UNVERIFIED** — nothing here is invented. Severity
scale: blocker / high / medium / low. Five rows are documented build
findings (F1–F3, F5–F6); F4 is the registration session, still pending —
captured live when it runs (SUBMISSION-CHECKLIST §H row 1).

| # | Task attempted | Steps taken | Expected vs. actual | Severity | Workaround | Actionable suggestion | Source |
|---|---|---|---|---|---|---|---|
| F1 | Exercise the Ring webhook contract before portal registration completed (hardware-free integration test). | Searched the official docs for a sandbox base URL, console webhook simulator, or SDK: getting-started glossary, API reference §10 Environment Configuration, testing docs (curl/Postman guidance). | Expected a sandbox or console simulator to point tests at; actual: **no sandbox base URL exists** (Environment Configuration is Production-only — `api.amazonvision.com`; staging means your own registered endpoints), the glossary's "Sandbox Environment" has no URL or activation path, and "There is no official Ring Partner SDK". | high | Contract-first: fixtures are Ring's published example payloads (`tests/fixtures/wire/` + `PROVENANCE.md`) replayed through the same edge a live delivery takes; the rules sanction the simulator path ("A physical Ring device is not required"). | Publish a sandbox base URL or a console webhook sender emitting signed sample events per type — hardware-less integrators would validate against Ring infrastructure instead of against docs. | STATUS.md §S1 Lane B; SUBMISSION.md §Challenges; REGISTRATION-GUIDE.md appendix |
| F2 | Derive event order from webhook timestamps (cross-event state machine). | Read the webhook reference examples; compared the motion example's `timestamp_readable` against its epoch-ms `timestamp`. | Expected the "readable" field to render UTC (or document its zone); actual: it is a **local-time rendering exactly 5 h off UTC** — undocumented; any string-time math silently corrupts event order. | medium | Parse only the epoch-ms `timestamp`, which the docs designate for time calculations; quirk pinned as a provenance-tagged fixture and regression-tested. | Render `timestamp_readable` in UTC (or document its zone) and mark the epoch-ms field canonical for time math in the examples themselves. | STATUS.md §S1 (pinned quirk); SUBMISSION.md §Challenges |
| F3 | Validate with real device events via the portal's Test section (staging users). | Read develop.html#test: up to 10 staging users via "Log in with Ring"; checked the requirements. | Expected a path to exercise events from a real device; actual: real-device testing requires a Ring account on an **active Ring Protection plan with registered devices**, and the docs warn **"Only US Located Devices: Currently we only support devices located in the US"** — not reachable from Peru without US hardware. | high | Documented-contract replay lane for the demo (rules-sanctioned); registration still unlocks the real signing key for a future live window. | A regional loaner/test-device program, or an official simulator emitting signed sample streams per event type, would open real-device validation to non-US integrators. | REGISTRATION-GUIDE.md §5 + §7; STATUS.md §S1 Lane B |
| F4 | Register the developer account + create the private app (S1 gate; runbook `docs/REGISTRATION-GUIDE.md`). | Runbook prepared from official docs (every step source-cited); portal visit **not yet attempted** as of 2026-09-17. | **UNVERIFIED — anticipated friction, documented ahead, none experienced yet**: identity verification requires passport photos (3 attempts; the docs' #1 failure cause is legal-name mismatch), app credentials are shown **once with no regenerate** (recovery = delete the app), and endpoint configuration demands four publicly-reachable HTTPS URLs (a dev laptop needs a tunnel). | expected low–medium (runbook research found no developer-region restriction — IDs accepted from 249 countries/territories — and no registration fee) | Runbook mitigations: match "Full legal name" to passport before submitting ID photos; download the credentials CSV immediately; configure staging endpoints behind a tunnel first. | TBD — captured live when the session runs; this row updates in place. | REGISTRATION-GUIDE.md §0–§4, §7–§8 — **UNVERIFIED** |
| F5 | Design honest HTTP responses for the documented event types the pipeline does not consume (8 of 10). | Read the webhook delivery semantics in the API reference while implementing the edge adapter (`src/ring_assistant/wire.py`). | Expected (REST instinct) 4xx to reject unhandled event types; actual: **Ring reads 4xx as permanent failure**, so unconsumed types must ack 200 and be ignored — only signature/schema failures may answer 4xx. | medium | `UnsupportedEvent` → `200 {"status": "ignored"}` in the live-wire server; structural violations stay `SchemaError` → 400. Same code path offline and live, so the demo cannot drift from production behavior. | State the ack-200-and-ignore contract for unconsumed types explicitly in the webhook reference, or offer per-subscription event opt-down so senders stop delivering unwanted types. | STATUS.md §S1 Lane B (wire adapter); SUBMISSION.md §Challenges |
| F6 | Verify Ring's webhook signatures at the edge (stage 0, `wire.py` + `verify.py`) before any body handling. | Read the webhook reference's signature section and its "Common Mistakes" list while implementing the edge; ordered the pipeline verify-then-parse; pinned the ordering with a re-serialization regression test. | Expected the signature to bind the payload's JSON content (two serializations of the same object verify alike); actual: `X-Signature: sha256=<hex>` is an HMAC over the **raw request bytes** — the docs' own #1 common mistake is parsing/re-encoding before MAC-ing — so any re-serialization (key order, indentation, framework body rewriting) fails every signature. | high | `receive_v1_1` MACs the untouched byte string before the first `json.loads`; a byte-different serialization of the same JSON fails closed (`test_signature_binds_raw_bytes_not_reparsed_json`, tests/test_wire.py). | State the byte-identity requirement where the header is defined, not only in a "Common Mistakes" appendix, and ship canonical verify-then-parse snippets; frameworks that expose a re-encoded body should surface the original bytes too. | docs/ARCHITECTURE.md §pipeline stage 0 (raw signed v1.1 bytes); STATUS.md §S1 Lane B (byte-identical scheme); SUBMISSION.md §Challenges; tests/test_wire.py §"signature over RAW bytes" |
