# A day at the door — 2026-05-15

One scripted day replayed through the live-wire pipeline (`Pipeline.handle_v1_1`: verify → normalize → snapshot-classify → state → route). Every beat is a real v1.1 envelope, signed as Ring would sign it; each classification consults a snapshot keyed `<device>@<epoch_ms>` — the same key the Image Snapshots API uses, served offline from the manifest.

Device `ava1.ring.device.door001` · 9 deliveries (including 1 redelivery) · 9 timeline entries · 6 notifications · generated by `uv run timeline`

## Timeline

| occurred | id | platform said | snapshot said | intent | action | transition | reason |
|---|---|---|---|---|---|---|---|
| 08:03:21.210 | s2-001 | motion_detected | package present, no person | package_deposited | notify | track_opened | intent=package_deposited |
| 08:04:02.805 | s2-002 | motion_detected/vehicle | vehicle body and wheels | vehicle_at_door | notify | no_track_change | intent=vehicle_at_door |
| 09:12:47.338 | s2-003 | ding | person at the door | person_at_door | notify | no_track_change | intent=person_at_door |
| 09:12:47.338 | s2-004 | ding | person at the door | person_at_door | suppress | duplicate | event ava1.ring.device.door001_button_press_1778836367338 already applied |
| 12:47:05.062 | s2-005 | motion_detected/human | person carrying a package | package_picked_up | notify | track_closed | intent=package_picked_up |
| 13:30:00.500 | s2-006 | motion_detected | — (no verdict; platform fallback) | motion_noise | suppress | no_track_change | lone unclassified motion (1/3 in window) |
| 13:34:18.120 | s2-007 | motion_detected | — (no verdict; platform fallback) | motion_noise | suppress | no_track_change | lone unclassified motion (2/3 in window) |
| 13:38:41.977 | s2-008 | motion_detected | — (no verdict; platform fallback) | motion_noise | notify | no_track_change | intent=motion_noise |
| 22:41:09.104 | s2-009 | ding | person at the door | person_at_door | escalate | no_track_change | intent=person_at_door night=True -> escalate all sinks |

The 08:03 row is the S2 story in one line: the platform's own vocabulary never says `package_delivery` on the live wire, so the snapshot is what turns unclassified motion into a deposit.

## Notifications (what would have gone out)

- **08:03:21 · Package deposited** (info) — ava1.ring.device.door001: a package was left at the door.
- **08:04:02 · Vehicle at the door** (info) — ava1.ring.device.door001: a vehicle is at the door.
- **09:12:47 · Person at the door** (info) — ava1.ring.device.door001: someone is at the door.
- **12:47:05 · Package picked up** (info) — ava1.ring.device.door001: the package was taken from the door.
- **13:38:41 · Repeated motion** (info) — ava1.ring.device.door001: 3 unclassified motions in the last 10 min — worth a look.
- **22:41:09 · Person at the door at night** (critical) — ava1.ring.device.door001: person detected at 22:41 (night window).

## Replay the day

```console
$ uv run replay-webhooks .timeline/webhooks.jsonl --snapshots .timeline
```

The capture is the same JSONL the live server writes under `RING_RECORD_DIR`, and `--snapshots` points the classifier at the manifest source — together they reproduce the table above row for row. Without `--snapshots` the replay classifies from platform fields alone (a wiring check: 08:03 reads as unclassified motion). On registration day the recorded traffic replaces the capture and `RING_API_TOKEN` replaces the manifest; the command is unchanged.

## Caveats

- Snapshots are the six deterministic synthetic scenes (`imaging.py`); the classifier thresholds are calibrated against them. Real Ring snapshots are watermarked JPEG, which the stdlib decoder refuses by design — those route to the LLM classifier (`llm.py`) instead of the pixel rules.
- `received_at` times are scripted (4 s after occurrence; the redelivery +85 s), not measured network delay. Occurrence times carry milliseconds because they are manifest keys, and the manifest keying is the API keying.
- Signatures use the demo secret (or `RING_WEBHOOK_SECRET` when set). The capture holds no secrets: replay re-signs with the local secret.
- `.timeline/state.db` holds the tracks the day derived; re-running the CLI regenerates the whole directory.
